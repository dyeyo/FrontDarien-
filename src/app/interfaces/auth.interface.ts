export interface LoginRequest {
    email: string;
    password: string;
}

export interface ReqiesterRequest {
    name: string;
    email: string;
    password: string;
    password_confirmation: string;
}